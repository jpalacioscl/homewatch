# Auto-generated by homewatch/scripts/generate_config.sh
# Hardware: Intel(R) Core(TM) 7 240H | RAM: 61,2GB | Storage: HDD
name: homewatch

services:

  # ── MQTT Broker ─────────────────────────────────────────────────────────────
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: mosquitto
    restart: unless-stopped
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    ports:
      - "1883:1883"
    networks:
      - homewatch

  # ── Frigate NVR ─────────────────────────────────────────────────────────────
  frigate:
    image: ghcr.io/blakeblackshear/frigate:stable-nvidia
    container_name: frigate
    restart: unless-stopped
    privileged: true          # needed for hardware access
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    shm_size: "256mb"         # shared memory for frame buffers
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./frigate/config.yml:/config/config.yml
      - frigate_data:/data
      - frigate_media:/media/frigate
    ports:
      - "5000:5000"           # Frigate web UI
      - "8554:8554"           # RTSP re-streams
      - "8555:8555/tcp"       # WebRTC
      - "8555:8555/udp"
    environment:
      FRIGATE_RTSP_PASSWORD: "changeme"
    mem_limit: 4g
    depends_on:
      - mosquitto
    networks:
      - homewatch

  # ── Home Assistant ───────────────────────────────────────────────────────────
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: homeassistant
    restart: unless-stopped
    privileged: true
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./homeassistant/config:/config
    ports:
      - "8123:8123"           # Home Assistant web UI
    environment:
      TZ: America/Santiago
    depends_on:
      - mosquitto
      - frigate
    networks:
      - homewatch

volumes:
  mosquitto_data:
  mosquitto_log:
  frigate_data:
  frigate_media:

networks:
  homewatch:
    driver: bridge

